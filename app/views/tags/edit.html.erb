  <h1>Tags#edit</h1>

  <%= form_for(@tag) do |f| %>
    <div class="field">
      <p>title</p>
      <%= f.text_field :title %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>