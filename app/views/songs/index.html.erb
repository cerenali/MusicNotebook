<h1>Songs#index</h1>

<table>
	<tr>
		<td>title</td>
		<td>artist</td>
	</tr>
	<% @songs.each do |song| %>
		<tr>
      <td><%= link_to song.title, song_path(song.id) %></td>
			<td><%= song.artist %></td>
      <td><%= link_to "delete song", song, data: { confirm: 'Are you sure?' }, method: :delete %> </td>
		</tr>
	<% end %>
</table>

<p><%= link_to "Add new song", new_song_path %></p>

<p><%= link_to "Back to users", users_path %></p>